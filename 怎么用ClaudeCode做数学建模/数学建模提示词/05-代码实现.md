进入代码实现阶段。使用 math-modeling skill 的编程手角色。

⚠️ 主代理没有多模态能力，不能直接读取或识别任何图片。遇到题目中的示意图、图表、手写公式等图像内容，必须派发子代理并指定 model: "opus" 处理。

前置读取：
- 读取编程手的角色文档：math-modeling skill 目录下的 references/roles/编程手/SKILL.md
  - 注意：SKILL.md 中的路径是相对于 math-modeling skill 目录的，以下步骤中的路径是完整路径
- 读取建模分析阶段产出的：题目分析报告.md、术语表格.md（如有读取不到的或是缺失的文件，一定要及时反馈！）

执行策略（在开始编码前确认）：

核心算法验证（按需 TDD）：
- 如果之前配置了 TDD 范围，对指定的核心算法函数使用 TDD
- 如果配置为"不需要 TDD"，则跳过，直接编写代码
- 其余脚本代码不做 TDD

并行执行：
- 如果有 >= 2 个相互独立的子问题（问题之间没有数据依赖），问用户是否使用 subagent-driven-development 并行求解
- 如果用户同意，为每个问题派发独立的子 agent，每个子 agent 完成后进行规格审查 + 质量审查
- 如果子问题不独立或只有 1 个问题，顺序执行即可

模型问题处理：
在编码和运行过程中持续检查模型合理性，发现问题立即暂停并告诉用户。

触发节点：
1. 实现公式时：维度/量纲不一致、除零风险、符号冲突、推导跳步
2. 运行代码后：结果出现 NaN/Inf/负数、不符合物理意义、数量级明显错误
3. 优化求解时：目标函数不收敛、约束无法同时满足、陷入局部最优
4. 对比验证时：和附件数据偏差 > 10%、多问题结果矛盾

处理方式：
- 问题明确（公式抄错、符号搞混）→ 直接修正，更新题目分析报告.md，继续
- 问题复杂（模型选型有误、需换方案）→ 暂停，说明问题和判断依据，等用户确认

⚠️ 必须严格按以下顺序逐步执行，不得跳步、合并或自行调整顺序。每完成一步后必须暂停等用户确认，确认后才能进入下一步。

步骤 1/6：读取 references/roles/编程手/references/工作流程.md
→ 按流程逐步执行：读取数据 → 编写求解代码 → 运行 → 输出结果表格
→ 实现公式时如需参考题目中的示意图或图表，必须派发子代理并指定 model: "opus"（mimo-v2.5，支持多模态）识别，主代理没有多模态能力不能直接识别图像
→ 数值精度要求：最终结果保留 4-6 位小数（和题目数据精度匹配），中间计算使用 Python 默认精度，图表标签保留 2-3 位小数
→ 完成后报告：代码文件名和运行结果概要
→ ⏸️ 暂停，等用户确认后进入下一步

步骤 2/6：读取 references/roles/编程手/references/常见模式.md
→ 参考图表代码模板（折线图、柱状图、热力图、三维曲面等）
→ 根据数据特点，向用户推荐图表方案（推荐 2-3 种），说明每种方案的适用场景和优劣
→ 等用户确认最终的图表数量和内容后，再进入下一步
→ ⏸️ 暂停，等用户确认后进入下一步

步骤 3/6：读取 references/roles/编程手/references/可视化规范.md
→ 获取项目级可视化配置：调色板、字体、坐标轴样式、导出规格
→ 完成后报告：已获取配置
→ ⏸️ 暂停，等用户确认后进入下一步

步骤 4/6：必须通过 Skill 工具调用 nature-figure skill（使用 Skill 工具，不要跳过这一步）
→ 使用 nature-figure 的完整工作流创建可视化图表：Figure Contract → 绘图 → QA → 导出
→ 按 SCI/Nature 标准绘图，使用步骤 3 的配置
→ 中文标签使用 SimHei 或 Microsoft YaHei 字体，确保不出现乱码
→ 保存为 SVG（可编辑矢量）和 PNG（高清位图）双格式
→ 如果需要视觉检查图表质量（标签重叠、颜色不清晰、数据标注错误），派发子代理并指定 model: "opus" 识别后修正
→ 完成后报告：生成的图表文件列表
→ ⏸️ 暂停，等用户确认后进入下一步

步骤 5/6：读取 references/roles/编程手/references/可视化面板模板.html
→ 基于模板生成 HTML 图表导航面板，整合 figures/ 目录下的所有图表（包括建模阶段的模型说明图和本阶段的数据可视化图）
→ 完成后报告：HTML 面板文件名
→ ⏸️ 暂停，等用户确认后进入下一步

步骤 6/6：读取 references/roles/编程手/references/质检清单.md
→ 交付前逐项检查：代码可运行、结果正确、图表规范、文件完整
→ 如果需要视觉检查图表是否规范，派发子代理并指定 model: "opus" 识别后确认
→ 完成后报告：质检结果（通过/有问题需修复）
→ ⏸️ 暂停，等用户确认后进入下一步

产出文件：
- 代码 → code/ 目录（如 code/问题1_求解.py）
- 结果 → results/ 目录（如 results/结果表格.csv）
- 图表 → figures/ 目录（如 figures/fig1_xxx.svg、figures/fig1_xxx.png）
- HTML面板 → figures/ 目录（如 figures/图表面板.html）