请完成数学建模的准备工作。

⚠️ 不要自动搜索或猜测文件名。必须直接问用户，等用户回答后再操作。
⚠️ 主代理没有多模态能力，不能直接读取或识别任何图片文件（.png/.jpg/.bmp等）。遇到图片必须派发子代理并指定 model: "opus" 处理。

一次性问用户以下三个文件的信息（不要分三次问）：
1. 题目文件名（不限格式：PDF/Word/图片等）
2. 格式要求文件名（不限格式：PDF/Word/图片等）
3. 附件数据文件名（Excel/CSV），如果没有附件数据用户会说"没有"

等用户回答后，依次执行：
- 根据文件扩展名自动选择工具读取题目文件：
  - PDF → 使用 pdf skill
  - Word (.docx) → 使用 docx skill
  - 图片 (.png/.jpg) → 必须派发子代理并指定 model: "opus"（mimo-v2.5，支持多模态）读取和识别，主代理没有多模态能力不能直接读取图片
  - PDF/Word → 先用对应工具读取文本部分，如果文件中包含示意图、图表、手写公式等图像内容，必须派发子代理并指定 model: "opus" 识别图像部分，主代理不能直接识别图像
  - 其他格式 → 告知用户，由用户决定处理方式
- 同样方式读取格式要求文件
- 如果有附件数据，使用 xlsx skill 读取（读取时注意中文编码，确保不出现乱码）

读取完成后，向用户确认以下信息：
- 题目有几个小问
- 每个小问的核心要求
- 附件数据的结构和规模（如有）
- 是否有已知答案或参考结果

确认无误后，等待用户指示下一步。